# Fix: Missing highlight section name

## Problem

When the user added a new highlight section, this section is empty, but the section name is not rendered.

## Plan

- Investigate the code responsible for handling highlight section (element)
- Identify any potential issues or missing functionality that could be causing the problem.
- If the problem is easy to fix, consider implementing a solution, otherwise ask for help.

## Validation

- Run `npm run lint` to check for lint errors
- Run `npm run tsc` to run the TypeScript compiler

## Result (100%)
