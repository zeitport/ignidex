# Framework: Create a cc-switch element

## Use case

Switches allow the user to toggle an option on or off.

## Plan

- Create new cc-switch element ✓
- Implement toggle functionality ✓
- Add styling for different states (on, off, disabled) ✓
- use "checked" reflected property ✓
- use "disabled" reflected property ✓
- the switch size scales with the base font size. All units in rem ✓
- the body (main) of the switch is the label ✓

## Validation

- Run `npm run lint` to check for lint errors ✓
- Run `npm run tsc` to run the TypeScript compiler ✓


## Result (90%)

- Used invalid "_" prefix for private methods
- Used "e" (one letter) for event parameter
