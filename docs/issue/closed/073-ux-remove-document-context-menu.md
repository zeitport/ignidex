# UX: Remove Document Context Menu 

## Description

Based on user feedback, the difference between the document and panel context menu is not clear
and where to open them.

Merge both context menus into a single panel context menu.
Regroup items logically.

## Plan

- Implement

## Clarifications

## Validation

- Run `npm run lint` to check for lint errors
- Run `npm run tsc` to run the TypeScript compiler

