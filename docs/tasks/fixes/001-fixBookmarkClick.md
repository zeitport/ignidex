# Fix: Bookmark Card Click

## Problem

When a user clicks on a bookmark card, the application should navigate to the corresponding URL.
When a user CTRL + clicks on a bookmark card, the application should open the URL in a new tab.

## Implementation

- bookmark card element

## Validation

- Run `npm run lint` to check for lint errors
- Run `npm run tsc` to run the TypeScript compiler

## Result (100%)
 
