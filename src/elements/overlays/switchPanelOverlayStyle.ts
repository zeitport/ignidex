import {css} from 'lit';

export const switchPanelOverlayStyle = css`
    .info-text {
        padding: 0.25rem;
        font-size: 0.8rem;
        color: var(--muted);
    }

    .info-url {
        word-break: break-all;
        color: var(--accent);
        font-weight: bold;
    }

    h2 {
        margin-block-end: 0.5rem;
    }
`;
